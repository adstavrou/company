{
  "name": "terra",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/terra/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Building ${libName}...'"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Running tests for ${libName}...'"
      }
    }
  },
  "tags": []
}
