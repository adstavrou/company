{
    "$schema": "http://json-schema.org/schema",
    "title": "Barrel Generator",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project (library) to generate the barrel for"
        }
    },
    "required": [
        "project"
    ]
}